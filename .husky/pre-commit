pnpm lint

pnpm test:unit
pnpm test:spec

if [ -n "$(git diff --name-only HEAD~1 | grep schema.prisma)" ]; then
    echo
    "⚠️ You modified the schema.prisma file.
    Make sure you also run the 'npm run db:migrate:dev' or 'docker:db:migrate:dev' command to generate the migration file.
    "
fi